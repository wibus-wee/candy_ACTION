name: candy Build

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: install node+yarn
          run: |
            actions/setup-node@v1
        - name: Test ok
          run: |
            node -v
            npm -v
            yarn -v

